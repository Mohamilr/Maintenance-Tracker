// dummyData
const dummyData = require('../utility/dummyData');

const Requests = require('../model/request.model');

const requestService{
    allRequests(){
        return dummyData.requests.map((request=>{
            const newRequest = new Requests();
            newRequest = Requests.id;
            newRequest = Requests.

        }))
    }
}